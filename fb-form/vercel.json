{
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "fb-form/dist/fb-form" } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
